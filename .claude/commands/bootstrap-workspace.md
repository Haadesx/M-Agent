Create or refresh a generated workspace for this project.

Use the current product brief, task, and any repo analysis already available.

Required outputs:
- project workspace files
- repo-aware slice agents when a repo path is present
- tool integration bundles

Stop only if required inputs are truly missing.
