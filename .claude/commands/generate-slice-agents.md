Generate markdown mini-agents for the current repository using the slice-agent-builder subagent.

Requirements:
- base them on the detected codebase slices
- make them directly usable by flagship coding agents
- keep each one bounded to a clear path and task slice
- write or update the relevant markdown files instead of only describing them
