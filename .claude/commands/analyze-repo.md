Analyze the repository at `$ARGUMENTS` using the repo-mapper subagent.

Required output:
- key files
- detected slices
- recommended specialists per slice
- risks or blind spots in the decomposition

If the path is missing, ask for a local repo path first.
