{
  "name": "agent-systems-control-plane",
  "version": "1.0.0",
  "private": true,
  "description": "A local-first product for generating markdown-native agent systems and tool integrations.",
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/generator.test.js && python3 -m unittest discover -s tests"
  }
}
